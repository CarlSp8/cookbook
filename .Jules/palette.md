## 2024-05-24 - API Key UX Insight
**Learning:** Users are often confused when an input requires an explicit action (like pressing Enter) without visual feedback or a clear submit button.
**Action:** When using implicit submission events like `.submit()` on a Gradio Textbox, always provide explicit instructions (e.g., using the `info` parameter).
